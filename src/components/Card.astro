---
const {title, imageAlt, imageUrl} = Astro.props;

---
<article>
  <img class="rounded-t-xl w-full" src={imageUrl} alt={imageAlt}>
  <div class="py-4 px-8 bg-primary rounded-b-xl">
    <h3 class="text-lg font-semibold mb-2">{title}</h3>
    <slot />
  </div>
</article>

